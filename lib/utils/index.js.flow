// @flow
const path = require('path')

const cwd: string = process.cwd()

function resolve (dir: string = '', parent: string = path.join(__dirname, '../')): string {
  return path.join(parent, dir)
}

module.exports = {
  cwd,
  resolve
}
