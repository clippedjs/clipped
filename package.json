{
  "private": true,
  "workspaces": [
    "packages/*",
    "generators/*",
    "presets/*"
  ],
  "scripts": {
    "commit": "git-cz",
    "test:all": "cross-env NODE_ENV='test' lerna run build && nyc --reporter=json --reporter=text mocha packages/*/test presets/*/test",
    "//test:all": "cross-env NODE_ENV='test' lerna run build && nyc --reporter=json mocha packages/*/test presets/*/test && remap-istanbul -i coverage/coverage-final.json -o coverage/coverage.json",
    "coverage": "nyc report --reporter=json > coverage.lcov && codecov"
  },
  "devDependencies": {
    "babel-runtime": "^6.26.0",
    "codecov": "^3.0.0",
    "commitizen": "^2.9.6",
    "cz-conventional-changelog": "^2.1.0",
    "lerna": "^2.5.1",
    "mocha": "^5.0.0",
    "nyc": "10.0.0",
    "remap-istanbul": "^0.10.1"
  },
  "config": {
    "commitizen": {
      "path": "node_modules/cz-conventional-changelog"
    }
  },
  "//nyc": {
    "sourceMap": false,
    "instrument": false
  }
}
